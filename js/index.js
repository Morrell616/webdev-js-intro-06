// Prevent us from attempting to use variables
// that are not declared
/*"use strict"

for (let i = 0; i<10; i++) {
    console.log(i);
}

let x = 0;
while (x < 8) {
    console.log(x);
    x++;
}
let r = 5;
do {
    console.log(r);
    r++;

} while (r < 15);
/*